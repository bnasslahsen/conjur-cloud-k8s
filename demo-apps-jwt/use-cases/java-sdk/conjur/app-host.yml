- !policy
  id: $K8S_PLATFORM-apps
  owner: !group managers
  body:
    - &hosts
      - !host
        id: $APP_JAVA_SDK-sa
        annotations:
          authn/api-key: true
    - !grant
      role: !group
      members: *hosts